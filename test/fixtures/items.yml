# N.B.: All items must specify their collection membership in
# collection_item_memberships.yml

################################ northeast ##################################

northeast_unit1_collection1_item1:
  institution:  northeast
  submitter:    northeast
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

################################## uiuc #####################################

# uiuc_collection1 items

uiuc_item1: # TODO: remove this
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_submitting:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::SUBMITTING %>
  stage_reason:

uiuc_submitted:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::SUBMITTED %>
  stage_reason:

uiuc_approved:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_awaiting_ingest_into_medusa:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_in_medusa:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_described:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_embargoed:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_undescribed:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

uiuc_withdrawn:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::WITHDRAWN %>
  stage_reason:

uiuc_buried:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::BURIED %>
  stage_reason:

uiuc_multiple_bitstreams:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:

# uiuc_collection1_collection1 items

uiuc_collection1_collection1_item1:
  institution:  uiuc
  submitter:    uiuc
  stage:        <%= Item::Stages::APPROVED %>
  stage_reason:
