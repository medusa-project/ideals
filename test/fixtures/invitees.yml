################################# example ###################################

example:
  institution:    example
  email:          example@example.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

example2:
  institution:    example
  email:          example2@example.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

example_sysadmin:
  institution:    example
  email:          admin@example.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

example_approved:
  institution:    example
  email:          approved-invitee@example.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

example_pending:
  institution:    example
  email:          pending@example.edu
  approval_state: <%= Invitee::ApprovalState::PENDING %>
  note:           This invitee is pending.
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

################################ northeast ##################################

northeast:
  institution:    northeast
  email:          user@northeast.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

northeast_admin:
  institution:    northeast
  email:          admin@northeast.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

################################ southwest ##################################

southwest:
  institution:    southwest
  email:          user@southwest.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

southwest_admin:
  institution:    southwest
  email:          admin@southwest.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

southwest_sysadmin:
  institution:    southwest
  email:          sysadmin@southwest.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

################################## uiuc #####################################

uiuc:
  institution:    uiuc
  email:          shib@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc2:
  institution:    uiuc
  email:          user2@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_admin:
  institution:    uiuc
  email:          instadmin@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_sysadmin:
  institution:    uiuc
  email:          sysadmin@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_unit1_admin:
  institution:    uiuc
  email:          uiuc_unit1_admin@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_unit1_unit2_unit1_admin:
  institution:    uiuc
  email:          uiuc_unit1_unit2_unit1_admin@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_collection1_collection1_manager:
  institution:    uiuc
  email:          uiuc_collection1_collection1_manager@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

uiuc_collection1_collection1_submitter:
  institution:    uiuc
  email:          uiuc_collection1_collection1_submitter@illinois.edu
  approval_state: <%= Invitee::ApprovalState::APPROVED %>
  note:           Sample note
  expires_at:     <%= Time.now + Invitee::EXPIRATION %>

