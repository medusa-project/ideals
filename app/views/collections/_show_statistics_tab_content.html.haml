%h2 Submissions

%table.table
  %tbody
    %tr
      %td Submitted Items
      %td= @num_submitted_items

%h2 Downloads

%table.table
  %thead
    %tr
      %th Item
      %th Count
  %tbody
    - @items.each do |row|
      %tr
        %td= link_to(row['title'], item_path(row['id']))
        %td= number_with_delimiter(row['dl_count'])
    %tr
      %th Total
      %th= number_with_delimiter(@items.map{ |row| row['dl_count'] }.sum)