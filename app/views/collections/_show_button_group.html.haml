.btn-group.float-right{role: "group"}
  // Delete button
  -if policy(@collection).destroy?
    = link_to(collection_path(@collection), class: 'btn btn-danger',
                                          method: :delete,
                                          "data-confirm": "Are you sure you want to delete this collection?") do
      %i.fa.fa-trash
      Delete