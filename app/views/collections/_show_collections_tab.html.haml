.btn-group.float-right{role: "group"}
  - if policy(@resource).edit_collection_membership?
    %button.btn.btn-light.edit-collection-membership{"data-collection-id": @resource.id,
                                                     "data-target": "#edit-collection-membership-modal",
                                                     "data-toggle": "modal",
                                                     role: "button"}
      %i.fas.fa-pencil-alt
      Change Collection Membership
    %button.btn.btn-light.add-sub-collection{"data-collection-id": @resource.id,
                                             "data-target": "#add-collection-modal",
                                             "data-toggle": "modal",
                                             role: "button"}
      %i.fa.fa-plus
      Add Child Collection

.clearfix

- if @resource.parent
  %h2 Parent Collection
  = resource_list([@resource.parent])

- if @subcollections.any?
  %h2 Child Collections
  = resource_list(@subcollections)
