.card.mb-3
  .card-body
    = form_tag(collection_downloads_path(@collection), class: "form-inline") do
      = date_range_picker

.mt-3
  = spinner

#downloads-xhr-content
  -# _show_downloads_tab_content inserted here via XHR