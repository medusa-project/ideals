-# @param institution [Institution]

= form_for(institution, url: institution_preservation_path(institution), remote: true) do |f|
  .error-messages
    -# populated via ajax by shared/_validation_messages.js.erb

  .mb-3
    = f.label :medusa_file_group_id, "Medusa File Group ID"
    = f.text_field :medusa_file_group_id, class: "form-control"
    %p.form-text
      = icon_for(:info)
      Every institution should have its own bit-level file group within its own
      collection.

  .mb-3
    = f.label :outgoing_message_queue, "Outgoing Message Queue"
    = f.text_field :outgoing_message_queue, class: "form-control"
    %p.form-text
      = icon_for(:info)
      A queue with this name must exist in RabbitMQ.

  .mb-3
    = f.label :incoming_message_queue, "Incoming Message Queue"
    = f.text_field :incoming_message_queue, class: "form-control"
    %p.form-text
      = icon_for(:info)
      A queue with this name must exist in RabbitMQ.

  .clearfix
    .float-end
      %button.btn.btn-light{"data-bs-dismiss": "modal", type: "button"} Cancel
      = f.submit("Save Changes", class: "btn btn-primary")
