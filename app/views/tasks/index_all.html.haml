- provide :title, 'All Tasks'
- provide :body_id, 'tasks'

%h1 All Tasks

= hidden_field_tag(:limit, @limit)

= render partial: "filter_form", locals: { show_institution: true }

#tasks-list
  = render partial: "tasks", locals: { show_institution: true }

= render partial: "shared/xhr_modal",
         locals: { id: "show-task-modal",
                   title: "Task" }
