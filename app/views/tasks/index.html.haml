- provide :title, 'Tasks'
- provide :body_id, 'tasks'

%h1
  Tasks
  %small
    in
    = current_institution.name

= hidden_field_tag(:limit, @limit)

= render partial: "filter_form", locals: { show_institution: false }

#tasks-list
  = render partial: "tasks", locals: { show_institution: false }

= render partial: "shared/xhr_modal",
         locals: { id: "show-task-modal",
                   title: "Task" }
