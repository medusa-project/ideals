-# frozen_string_literal: true
= simple_form_for(@resource) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :title
    = f.input :parent_unit_id

  .form-actions
    = f.button :submit
