.btn-group.float-right{role: "group"}
  - if @unit.default_collection && policy(@unit.default_collection).submit_item?
    = link_to(collection_submit_path(@unit.default_collection), class: "btn btn-light", role: "button") do
      %i.fas.fa-plus
      Submit Item To This Unit
  - if policy(@unit).export_items?
    .dropdown
      %button#export-button.btn.btn-light.dropdown-toggle{type:            "button",
                                                          "data-toggle":   "dropdown",
                                                          "aria-expanded": "false"}
        %i.fa.fa-download
        Export
      .dropdown-menu.dropdown-menu-right{"aria-labelledby": "export-button"}
        = link_to(unit_items_path(@unit, format: :csv), class: "dropdown-item") do
          All Items As CSV

.clearfix.mb-3

- if @items.count.positive?
  = form_tag(unit_path(@unit), method: :get, class: "filter") do
    .row.justify-content-between
      .col-6
        .input-group.mb-3.filter-field
          = hidden_field_tag("fq[]", "#{Item::IndexFields::UNIT_TITLES}:#{@unit.title}")
          = filter_field(icon: "fa fa-filter", placeholder: "Filter")
      .col-6
        .input-group
          = sort_menu(MetadataProfile.default)
  #items-xhr-content
    -# items/listing inserted here via XHR
- else
  This unit does not contain any items.
