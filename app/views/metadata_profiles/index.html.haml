- provide :body_id, "metadata_profiles"
- provide :title, "Metadata Profiles"

.btn-group.float-end{role: "group"}
  -# Add Profile button
  %button.btn.btn-light{"data-bs-target": "#add-profile-modal",
                        "data-bs-toggle": "modal",
                        type:             "button"}
    %i.fa.fa-plus
    Add Profile

%h1 Metadata Profiles

%table.table
  %thead
    %tr
      %th Name
  %tbody
    - @profiles.each do |profile|
      %tr
        %td
          = link_to(profile.name, profile)
          - if profile.institution_default
            %span.badge.text-bg-success DEFAULT

= render partial: 'add_modal'
= render partial: "shared/ajax_modal",
         locals: { id: "edit-profile-modal", title: "Edit Profile" }

