.btn-group.float-right{role: "group"}
  / Cancel button
  = link_to(item_cancel_submission_path(@resource), class: "btn btn-danger",
                                                    role: "button",
                                                    method: :delete,
                                                    data: { confirm: "Are you sure you want to cancel this submission? Once canceled, it cannot be retrieved!" }) do
    %i.fa.fa-stop
    Cancel

%h1 Deposit

%p Deposit form goes here